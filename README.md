# AQN2013

A recreation of 2013 AQN based on a leaked database backup.

## Misc notes

When importing a database backup, make sure it is in the right encoding. If needed, prefix the database dump with `/*!40101 SET NAMES utf8 */;`.
